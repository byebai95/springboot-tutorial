<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="app.mapper.UserMapper">

    <select id="getUserList" resultType="app.model.User">
        select * from t_user
    </select>
</mapper>