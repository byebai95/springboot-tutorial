server:
  port: 8080

swagger:
  title: "JWT Token 验证项目"
  version: "1.0.0"
  basePackage: "app.controller"

spring:
  redis:
    host: localhost
    port: 6379
    password:
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
